# .NET
bin/
obj/
*.user
*.suo
*.cache
.vs/
artifacts/
publish/
TestResults/
coverage*.xml
*.coverage
*.coveragexml

# IDE
.idea/
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
*.swp

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
.env.local
.env.development.local
.env.production.local

# Secrets y runtime local
*.pfx
*.pem
*.key
appsettings.Development.local.json
appsettings.Local.json
secrets.json
.userprofile/

# Logs
logs/
*.log

# OS
Thumbs.db
.DS_Store

# Datos locales del satélite y backups
data/
buffer.db
buffer.db-shm
buffer.db-wal

# F11.4 — tarballs del binario del satélite servidos por GET /api/satellite/binary.
# Son output del build; los regenera scripts/publish-satellite.sh.
/apps/api/satellite-binaries/
/build/satellite/

# Docker
docker-compose.override.yml

# Secretos locales / smoke testing
**/.aspnet-keys/
cookies.txt
login.json
/tmp/
/apps/api/logs/

# Contexto operativo local — NUNCA al repo (credenciales reales, IPs, llaves).
# Sin esta red, un `git add -A` distraido los publica.
AETHRA-HANDOFF.md
AETHRA-DEPLOY-CONTEXT.md
appsettings.Local.json
**/appsettings.Local.json

# Secretos del compose: .env.example se versiona, .env NUNCA.
.env
deploy/.env

# Bitacoras internas de despliegue: nombran VMs, dominios y clientes reales.
docs/F13-compose-native-deploy.md

# Salida de compilacion: se regenera con scripts/publish-satellite.sh
# (el endpoint /api/satellite/binary sirve desde apps/api/satellite-binaries/, no de aqui)
build/
